apiVersion: v1
kind: Secret

metadata:
  name: mysql-secret
  namespace: employee-app

type: Opaque

stringData:
  MYSQL_USER: {{ .Values.mysql.user | quote }}
  MYSQL_PASSWORD: {{ .Values.mysql.password | quote }}