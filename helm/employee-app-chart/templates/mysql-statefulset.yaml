apiVersion: apps/v1
kind: StatefulSet

metadata:
  name: mysql
  namespace: employee-app

spec:
  serviceName: mysql-service

  replicas: {{ .Values.mysql.replicas }}

  selector:
    matchLabels:
      app: mysql

  template:
    metadata:
      labels:
        app: mysql

    spec:
      initContainers:
      - name: clean-lost-found

        # Fresh EBS volumes are ext4-formatted with a reserved lost+found/
        # directory, which makes MySQL's --initialize step think the
        # datadir isn't empty and abort. Clearing it is safe on every
        # restart since it only ever removes that reserved directory.
        image: busybox:1.36
        command: ["sh", "-c", "rm -rf /var/lib/mysql/lost+found"]

        volumeMounts:
        - name: mysql-storage
          mountPath: /var/lib/mysql

      containers:
      - name: mysql

        image: "{{ .Values.mysql.image.repository }}:{{ .Values.mysql.image.tag }}"

        args:
        - --default-authentication-plugin=mysql_native_password
        - --ssl=0

        ports:
        - containerPort: 3306

        env:

        - name: MYSQL_ROOT_PASSWORD
          value: "{{ .Values.mysql.rootPassword }}"

        - name: MYSQL_DATABASE
          value: "{{ .Values.mysql.database }}"

        - name: MYSQL_USER
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: MYSQL_USER

        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: MYSQL_PASSWORD

        volumeMounts:
        - name: mysql-storage
          mountPath: /var/lib/mysql

        - name: initdb
          mountPath: /docker-entrypoint-initdb.d

      volumes:
      - name: initdb
        configMap:
          name: mysql-initdb-config

  volumeClaimTemplates:
  - metadata:
      name: mysql-storage

    spec:
      accessModes:
      - ReadWriteOnce

      storageClassName: ebs-sc

      resources:
        requests:
          storage: {{ .Values.mysql.storage }}