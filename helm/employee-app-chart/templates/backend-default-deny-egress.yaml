apiVersion: networking.k8s.io/v1
kind: NetworkPolicy

metadata:
  name: backend-default-deny-egress
  namespace: employee-app

spec:
  podSelector:
    matchLabels:
      app: backend

  policyTypes:
    - Egress